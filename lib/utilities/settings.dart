import 'package:flutter/material.dart';

const warnaCerah = Colors.white;
const tambahBayangan = [Shadow(
                    blurRadius:  4.0,
                    color:  Color.fromARGB(98, 0, 0, 0),
                    offset : Offset(0,6),
                  )];
const tambahBayanganGaris = <BoxShadow>[
                    BoxShadow(
                        color: Colors.black54,
                        blurRadius: 1.0,
                        offset: Offset(0, 4))
                  ];